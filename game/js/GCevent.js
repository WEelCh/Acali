
class GCevent { static Log = new Log( "GCevent" , "g" )

    static CW_allowed = false;

    static allSubevents = []; // sort into categories, remove CW and disabled ones

    /*
    
                if ( event.head.spawn.disabled ) { continue }
                if ( !GMap.NSFW && event.head.spawn.nsfw ) { continue }
    */

}
