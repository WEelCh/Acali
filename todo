
EVENT LOOP AND TARGETING

difficulty ( loot, degradation, env, severity )

using weatherObj, timeObj ????
using event object

GCevent ( weather subevents are global )
GCsound fire

Localization ( Mediator, Errors ) 
Mediator: game start settings looks trash

modtool: cards, events, weather, map --> started implementing "spawn new event"

* Storyteller as default difficulty settings (RimWorld-esc)
* () ersetzt ()()
* not defect catory on items, but special icons instead (or to less return from more complexity?)
* items destroyed by rain?
* start loot in chests?

MEDIATOR: import all js modules ??? --> data intensive (need to check)


+ yield could be emergent property of tag system ??? (instead of three different resource yield ranges)
+ event difficulty system (percentage vaues instead of range ?)


trapping as subevent of hunting (trap is crafted there)
trap time and status is tracked -> used for events (trap still empty / broken / rotten)

special items 


magic sysem: magic user selects secrete magic item, magic item with eye more better ?

tile: spawn on coast, wrecked ship

keyword: signal, digging